# 移动端重构历史记录

本目录保存 2026-07-06 左右移动端 PWA 重构的方案、交付清单、测试记录和阶段性总结，主要用于追溯当时的设计意图。

当前项目定位已经收敛为：桌面端是主体验，移动端只是个人手机 PWA 入口。移动端运行和安装说明以仓库根目录 `PWA.md` 为准；技术栈和项目边界以 `AI_PROJECT_CONTEXT.md` 与 `apps/web/src/features/mobile/README.md` 为准。

注意：

- 本目录内出现的 `8000`、`100.94.*`、局域网 IP 或旧临时地址属于历史记录。
- 本目录内的“已完成”“待实现”“执行顺序”等描述不等于当前代码状态。
- 新任务不要直接按这些旧计划批量改代码；应先读取当前源码、`PWA.md` 和主上下文文档。
