对全局的计时配置进行这样处理：即使退出页面，或者离开那个场景，虽然会触发结算时间到数据库，也就是现在已经实现的，但是后台仍然保持计算闲置时间，在闲置时间达到阈值前如果再度返回页面则接着计时，最终的记录也只有一条，而不是重进页面就要从头开始计时，你可以向我确定影响范围，我要的是全局任何场景的计时都这样

缩小为胶囊后不能变回卡片，出现休息询问的时候用户可能马上切回学习页面学习了，这时候就要自己切回学习计时，这个之前喊着做过但没做成功，还是保持在休息页面，我在随心模式里面答题也没自动切回，随心模式也是算学习的