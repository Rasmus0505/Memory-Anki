## 需求1 — 修复知识大纲导图保存失败

### 原始输入
用户在知识大纲页面编辑思维导图时保存失败，前端反复报 `PUT /api/v1/subjects/4/editor 500`，并提供了浏览器报错截图与调用栈。


## 需求2 — 项目模块化单体 Monorepo 重构

### 原始输入
用户要求把当前项目直接重构为高度模块化、低耦合、高内聚、易维护、易扩展的 monorepo 结构，后端改为模块化单体，前端拆成学习端和管理端两个应用，并统一共享契约与公共包。

