## 需求1 — 双链全局搜索简化与预览高亮

### 原始输入
双链全局搜索现在信息太杂，搜索结果希望只保留两层结构：一级显示宫殿，二级直接显示命中的正文内容，不要再出现路径、Lx 标记、路径标签、虚线嵌套层级和原样的 HTML 符号。搜索结果点预览时，希望在只读脑图里把当前搜索关键词标红；从 bilink badge 或 bilink 面板进入预览时不要带这个高亮。

### plan需求输入
统一编辑页和复习页的全局搜索展示，按宫殿分组后直接显示清洗过的命中文案，去掉路径树和各种层级标记；搜索结果进入预览时，在脑图预览里把当前搜索词标红，其他入口进入预览时不启用该高亮。

## 需求2 — 修复编辑页宫殿首开脑图错乱

### 原始输入
用户反馈宫殿编辑页有一个持续很久的老问题：有时首次打开宫殿时，脑图会出现显示错乱、平铺、错位或缩放漂移，希望尽快修复这个偶发但影响很明显的首开 bug。

### plan需求输入
修复 `/palaces/:id/edit` 主宫殿脑图在首次打开时偶发出现的错位、平铺和缩放漂移问题，让编辑页首开稳定以一致视角完成加载，同时不要污染知识大纲 iframe，也不要让旧的异步加载结果覆盖当前脑图状态。

## 需求3 — 宫殿编辑 AI续写改为 AI分卡

### 原始输入
脑图编辑时，选中卡片右键弹出的功能栏里，彻底删除最下面的 AI 续写，改成 AI 分卡；它要调用 qwen 3.6 flash 把当前节点拆成多个并列小节点。如果当前节点已经有子节点，旧子节点只允许整体搬家、不能改字，并且要由 AI 判断分别挂到新的分类下面。个人中心侧边栏还要新增独立的 AI 分卡配置页，支持 API Key、Base URL、模型、温度、最大分类数、是否携带备注、自定义附加说明等全量接入配置。

### plan需求输入
在宫殿编辑页脑图 iframe 宿主里，把节点右键功能栏底部原生 AI 续写替换成 AI 分卡，并接到新的前后端链路；后端只返回变换后的 editor_doc，不直接保存，前端继续走现有自动保存链路。AI 分卡需要把目标节点拆成新的一级分类，并在有旧子节点时把这些旧子节点整体重挂到新分类下面，漏分的节点自动补到“待归类”。个人中心改成桌面端左侧导航布局，并新增独立的 `/profile/ai-split` 配置页，沿用现有 review settings 存储这些分卡模型配置。

## 需求4 — 宫殿编辑页模式切换记忆焦点与按钮重命名

### 原始输入
宫殿编辑页里面切换复习练习双模式的时候，每次切换的焦点就回到复习和练习上次焦点地地方，比如上次点击了练习页面某个地方，然后切了复习页面，再切回去练习页面的话视图中心为上次点击地地方，另外将复习练习这个切换按钮的“复习”改为“编辑”，绿色代表练习，蓝色代表编辑。

### plan需求输入
宫殿编辑页里的页内双模式切换改成“编辑 / 练习”语义，并让编辑态和练习态分别记住各自上次停留的画布中心与节点焦点；首次从编辑切到练习沿用当前视图，之后切回任一模式都恢复到该模式上次的位置。切换按钮保持单个入口，练习显示为绿色，编辑显示为蓝色。

## 需求5 — 修复知识树编辑器切换学科后脑图不刷新

### 原始输入
知识树编辑器我切换不同学科怎么脑图没有变化

## 需求6 — 排查中外教育史大纲串题

### 原始输入
为什么中国和外国教育史的大纲混在一起了

## 需求6 — 正式复习页内联编辑切换

### 原始输入
再正式复习上方的工具栏新增编辑/复习的切换按钮，方便复习的时候发现问题顺手回去编辑，参考编辑时的练习/编辑切换视图方法，编辑时间算在复习里面。随后用户要求按已确认方案直接实现：只覆盖普通正式复习页，在同一页内提供“编辑 / 复习”切换，复用正式宫殿脑图保存链路，并保持整段时间继续记在 `review` 会话里。

## 需求7 — 修复知识树导入应用与流式输出跟随

### 原始输入
1.为什么点击脑图追加到节点或者覆盖都没有反应，修复bug
2.模型流式输出的时候窗口跟着下滑，可以一直看见显示最新的内容
