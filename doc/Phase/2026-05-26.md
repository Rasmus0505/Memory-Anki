## 需求1 — 宫殿双向链接与全局搜索

### 原始输入
为 Memory Anki 设计并落地宫殿/节点之间的双向链接能力，让用户在编辑脑图时能通过 `@` 快速搜索其他宫殿或节点并建立关联，同时支持工具栏全局搜索、节点反向链接徽标、上下文预览浮窗，以及侧边栏链接面板。

## 需求2 — 统一复习模式与编辑模式的半屏全屏视图

### 原始输入
复习模式下的半屏和全屏视图不应该和编辑模式不一致，需要把复习模式的半屏/全屏布局统一成和编辑模式相同的视图表现。

## 需求3 — 修复复习节点时间错排并统一提前复习锚定规则

### 原始输入
修复正式复习节点时间安排错误的问题，统一宫殿复习、分块复习、默认分块与修复/重建流程的排程逻辑，避免旧的 future pending 节点被错误保留，并让“提前复习锚定策略”真正生效，同时把已有排错的未来复习节点自动修正。

## 需求4 — 本地自动管理员登录、启动提速与学习界面优化

### 原始输入
优化 `D:\3.3-19.01\start-local.bat` 的本地启动体验：每次本地 SQLite 启动都自动校准并登录 `root@qq.com / 123123` 管理员账号，无需手动输入；为 web/admin 增加按源码变更跳过静态构建的缓存机制，减少重复启动耗时；把阅读页历史记录从当前只显示很少条目的横向区域改成固定高度、可滚动的多列卡片墙；同时修复学习模式中原本用于遮挡字幕的半透明遮挡板入口，让用户能在右下区域重新打开和关闭该功能。

## 需求5 — 本地生成链路修复与视频实测

### 原始输入
本地上传生成素材时报错 `创建云端任务失败；Method Not Allowed`，需要修复完整生成链路，并用 `C:\Users\Administrator\Desktop\A摩登\第二季01.mp4` 实际生成一份可学习课程，生成内容只勾选中文翻译。同时还要继续确认学习模式字幕遮挡板和历史记录新排版已经真正生效。

## 需求6 — 严格还原 PDF 脑图结构与全局导入配置

### 原始输入
为学科 PDF 转脑图新增严格还原模式：基础脑图结构必须与 PDF 自带脑图完全一致，原节点原话、层级、顺序和粒度都不能被 AI 改写、概括、合并或重排；正文补充只能挂到最小原始节点下，且也必须使用原话。导入时还要支持保留下划线与波浪线等强调样式，若结构无法严格一致则只生成近似草稿预览、不能覆盖或追加。以上策略需在导入侧边栏可配置，并记为全局默认，同时确保知识树页和宫殿编辑页都能看到并复用这套 PDF 导入能力。

## 需求7 — 学科转脑图 PDF 悬浮窗与识别结果可见性优化

### 原始输入
将学科转脑图左侧小栏改成中间更大的悬浮窗，把 PDF 缩略图单独放到悬浮窗右侧方便查看和选择；已分析过的 PDF 页面要用绿色标记，并记住上次浏览到的 PDF 页面，不要每次都从第一页重新翻。同时修复 PDF 转脑图识别后看不见结果、无法追加或覆盖、历史记录不明显的问题，让识别结果区、操作按钮和历史记录入口稳定可见。
