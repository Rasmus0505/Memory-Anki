---
编号: 05-12
标题: 备份保留策略与恢复入口优化
类型: 优化
范围: 功能
优先级: P1
预估工作量: M
依赖文档: 无
状态: 已完成
负责代理: Codex
完成时间: 2026-07-09
---

# 05-12 备份保留策略与恢复入口优化

## 1. 原始需求

备份恢复入口需要准确识别数据库快照，缺失数据库文件时应给出明确错误，并覆盖保留策略 prune 行为。目标是补齐生命周期服务与路由错误处理。

## 2. 详细执行清单

1. 修改 `apps/api/src/memory_anki/modules/backups/application/backup_lifecycle.py`。
2. 让 `has_database` 支持 `data/memory_palace.db` 与 manifest database path。
3. `restore_database_backup` 在缺失数据库快照时抛出 `FileNotFoundError`。
4. `apps/api/src/memory_anki/modules/backups/presentation/router.py` 将该错误映射为 400。
5. 补 `apps/api/tests/test_backup_lifecycle.py`。

## 3. 测试验收标准

- `cd apps/api && python -m pytest tests/test_backup_lifecycle.py tests/test_verify_backup_tool.py tests/test_full_transfer_service.py -q` 通过。
- `cd apps/api && python -m ruff check src/memory_anki/modules/backups tests/test_backup_lifecycle.py` 通过。

## 4. 进度记录（交接用）

| 时间 | 执行者 | 动作 | 结果/备注 |
|---|---|---|---|
| 2026-07-09 | Codex | 补备份识别、恢复错误和 prune 测试 | 已完成 |
