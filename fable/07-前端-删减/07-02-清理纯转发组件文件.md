---
编号: 07-02
标题: 删除/收敛三个纯转发（re-export）文件：ProfilePage.tsx、ProfileAiConfigPage.tsx、palaceQuizApi.ts
类型: 删减
范围: 前端
优先级: P2
预估工作量: S（<2h）
依赖文档: 无
状态: 已完成
负责代理: fable Worker 25
完成时间: 2026-07-09
---

## 1. 原始需求

三个文件本体几乎不含逻辑，只做转发，徒增一层跳转和维护成本（已逐个用 rg 核实引用面）：

1. `apps/web/src/features/profile/ProfilePage.tsx`（6 行）：全部是 re-export。唯一生产引用是 `apps/web/src/app/router/appRoutes.tsx:21` 的 `import('@/features/profile/ProfilePage')`，且只用其 default 导出（= `ProfileSettingsPage`）；其余 5 个具名导出（`ProfileSettingsPage/ProfileTimerPage/ProfileAiPage/ProfileBackupsPage/ProfileFeedbackPage`）无任何消费方——appRoutes 里这几个页面都是直接从各自文件 lazy import 的（appRoutes.tsx 第 47-52 行）。
2. `apps/web/src/features/profile/ProfileAiConfigPage.tsx`（1 行）：`export { AiWorkspacePage as ProfileAiConfigPage } from '@/features/profile/AiWorkspacePage'`。引用方仅 2 个：`features/profile/ProfileAiPage.tsx:4` 与 `features/profile/ProfileAiConfigPage.test.tsx:4`。
3. `apps/web/src/features/palace-quiz/api/palaceQuizApi.ts`（49 行）：前 32 行是对 `@/entities/quiz/api`（13 个函数）、`@/entities/knowledge/api`（2 个函数）、`@/entities/palace/api`（1 个函数）的纯转发，仅第 34-48 行的 `loadPalaceQuizPageData` / `loadPalaceQuizQuestions` 是真逻辑。它通过 `features/palace-quiz/api/index.ts`（`export * from './palaceQuizApi'`）被 8 个生产文件 + 1 个测试 mock 消费。

目标：删掉前两个文件、把第三个瘦身为只含真逻辑，消费方改为直连真实来源。FSD 边界不受影响（features 允许 import entities）。

## 2. 详细执行清单

> 不要做什么：不要重命名 `ProfileSettingsPage`/`AiWorkspacePage` 组件本身；不要动 `entities/quiz|knowledge|palace` 下的任何文件；不要合并或改写 `features/palace-quiz/api/index.ts` 之外的导出结构；不要顺手改任何页面逻辑。

### 第一部分：删除 ProfilePage.tsx

1. 核实引用：`cd apps/web && rg -n "features/profile/ProfilePage" src`
   - 期望结果：只有 `src/app/router/appRoutes.tsx:21` 一条（若出现其他文件，先逐个处理再继续）。
2. 打开 `apps/web/src/app/router/appRoutes.tsx`，把第 21 行改为直连真实页面：

   修改前：

   ```tsx
   export const preloadProfilePage = () => import('@/features/profile/ProfilePage')
   ```

   修改后：

   ```tsx
   export const preloadProfilePage = () => import('@/features/profile/ProfileSettingsPage')
   ```

   - 自查点：`ProfileSettingsPage.tsx` 有 default 导出（`ProfilePage.tsx` 第 1 行就是转发它的 default），所以第 46 行 `const ProfilePage = lazy(preloadProfilePage)` 无需改动。
3. 再次安全检查：`cd apps/web && rg -n "profile/ProfilePage" src`
   - 期望结果：**空输出**。
4. 删除文件 `apps/web/src/features/profile/ProfilePage.tsx`。

### 第二部分：删除 ProfileAiConfigPage.tsx

5. 核实引用：`cd apps/web && rg -n "ProfileAiConfigPage" src`
   - 期望结果：仅 3 个文件——本体、`ProfileAiPage.tsx`、`ProfileAiConfigPage.test.tsx`。
6. 打开 `apps/web/src/features/profile/ProfileAiPage.tsx`，改第 4 行 import 与第 40 行 JSX：

   修改前：

   ```tsx
   import { ProfileAiConfigPage } from '@/features/profile/ProfileAiConfigPage'
   // ...
   {tab === 'prompts' ? <ProfileAiPromptsPage /> : <ProfileAiConfigPage />}
   ```

   修改后：

   ```tsx
   import { AiWorkspacePage } from '@/features/profile/AiWorkspacePage'
   // ...
   {tab === 'prompts' ? <ProfileAiPromptsPage /> : <AiWorkspacePage />}
   ```

7. 打开 `apps/web/src/features/profile/ProfileAiConfigPage.test.tsx`，把第 4 行 import 改为 `import { AiWorkspacePage as ProfileAiConfigPage } from '@/features/profile/AiWorkspacePage'`（保留别名，测试内部 246/336/397 行的 `<ProfileAiConfigPage />` 无需改动）。**不要重命名测试文件**（最小改动）。
8. 安全检查：`cd apps/web && rg -n "from '@/features/profile/ProfileAiConfigPage'" src`
   - 期望结果：**空输出**。
9. 删除文件 `apps/web/src/features/profile/ProfileAiConfigPage.tsx`。

### 第三部分：瘦身 palaceQuizApi.ts

10. 核实消费方：`cd apps/web && rg -n "from '@/features/palace-quiz/api'" src`
    - 期望结果：8 个生产文件（`usePalaceQuizResources.ts`、`usePalaceQuizPractice.ts`、`usePalaceQuizManagement.ts`、`usePalaceQuizGeneration.ts`、`usePalaceQuizGenerationInputs.ts`、`PalaceQuizPage.tsx`、`features/freestyle/components/FreestyleAiExplainSheet.tsx`、`features/freestyle/FreestylePage.tsx`）。另有 `FreestylePage.test.tsx:52` 用 `vi.mock('@/features/palace-quiz/api', ...)`。
11. 逐个改消费方 import 来源（只改 import 语句的 from 部分，函数名不变）。转发函数的真实来源：`getSubjectTreeApi`/`getSubjectsApi` 来自 `@/entities/knowledge/api`；`getPalaceApi` 来自 `@/entities/palace/api`（palaceQuizApi.ts 内部使用，无外部消费方需要改）；其余全部来自 `@/entities/quiz/api`。逐文件修改：
    - `hooks/usePalaceQuizPractice.ts` 第 5 行：`from '@/features/palace-quiz/api'` → `from '@/entities/quiz/api'`（两个函数都来自 quiz）。
    - `hooks/usePalaceQuizManagement.ts` 第 6-11 行：同上改为 `from '@/entities/quiz/api'`。
    - `hooks/usePalaceQuizGeneration.ts` 第 10-13 行：同上改为 `from '@/entities/quiz/api'`。
    - `hooks/usePalaceQuizGenerationInputs.ts` 第 13-16 行：`getSubjectTreeApi, getSubjectsApi` 改为 `from '@/entities/knowledge/api'`。
    - `PalaceQuizPage.tsx` 第 10 行：`resetPalaceQuizQuestionAttemptsApi` 改为 `from '@/entities/quiz/api'`。
    - `features/freestyle/components/FreestyleAiExplainSheet.tsx` 第 4 行：`requestPalaceQuestionExplainApi` 改为 `from '@/entities/quiz/api'`。
    - `features/freestyle/FreestylePage.tsx` 第 72-75 行：`recordPalaceQuizChoiceAttemptApi, requestPalaceShortAnswerFeedbackApi` 改为 `from '@/entities/quiz/api'`。
    - `hooks/usePalaceQuizResources.ts` 第 3 行：**不改**（它用的 `loadPalaceQuizPageData`/`loadPalaceQuizQuestions` 是真逻辑，留在 features api）。
    - 自查点（每改一个文件跑一次）：`cd apps/web && npm run typecheck`。
12. 处理测试 mock：打开 `apps/web/src/features/freestyle/FreestylePage.test.tsx` 第 52 行的 `vi.mock('@/features/palace-quiz/api', ...)`，查看其 mock 的函数名。因为第 11 步已把 FreestylePage 的两个函数改从 `@/entities/quiz/api` 导入，需把这个 mock 改为 `vi.mock('@/entities/quiz/api', ...)`（保留原有的 mock 函数实现体不动；若 mock 对象里还有 FreestyleAiExplainSheet 用到的 `requestPalaceQuestionExplainApi`，一并保留在新 mock 中）。
    - 自查点：`cd apps/web && npx vitest run src/features/freestyle/FreestylePage.test.tsx` 全绿。
13. 打开 `apps/web/src/features/palace-quiz/api/palaceQuizApi.ts`，删掉第 1-32 行的转发 import/export 块，只保留真逻辑及其所需 import：

    修改后（完整文件）：

    ```ts
    import { getPalaceApi } from '@/entities/palace/api'
    import { getPalaceQuizQuestionsApi } from '@/entities/quiz/api'

    export async function loadPalaceQuizPageData(palaceId: number) {
      const [palace, questions] = await Promise.all([
        getPalaceApi(palaceId),
        getPalaceQuizQuestionsApi(palaceId),
      ])
      return {
        palace,
        questions: questions.items || [],
      }
    }

    export async function loadPalaceQuizQuestions(palaceId: number) {
      const result = await getPalaceQuizQuestionsApi(palaceId)
      return result.items || []
    }
    ```

14. 最终安全检查：`cd apps/web && npm run typecheck`
    - 期望结果：0 错误。若报"has no exported member"，说明第 11 步漏改了某个消费方，按报错文件补改。

## 3. 测试验收标准

- `cd apps/web && npm run typecheck && npm run test && npm run lint && npm run build` → 全部通过。
- 行为验收：
  - 访问 `/profile` → 设置页正常渲染（等同旧 ProfilePage default）；
  - 访问 `/profile/ai` → 默认 tab 显示 AI 工作台（AiWorkspacePage），切到 prompts tab 正常；
  - 访问 `/palaces/{某个存在的id}/quiz` → 题目列表加载、选择题作答后统计刷新；
  - 访问 `/freestyle` → 卡片流出题、选择题作答、简答题 AI 点评入口可点。
- 回归检查：`ProfileAiConfigPage.test.tsx`、`FreestylePage.test.tsx`、palace-quiz 相关测试全绿；eslint-plugin-boundaries 无新增违规（features→entities 是合法方向）。

## 4. 进度记录（交接用）

| 时间 | 执行者 | 动作 | 结果/备注 |
|---|---|---|---|
| 2026-07-08 22:30 | 文档撰写代理 | 文档创建 | 已核实三个文件的全部引用面（rg），消费方清单见第 10-12 步 |
| 2026-07-09 | fable Worker 25 | 执行低风险首批 | 删除 `ProfilePage.tsx`、`ProfileAiConfigPage.tsx`；`palaceQuizApi.ts` 仅保留真实加载函数；消费方直连 `entities/quiz|knowledge/api`；`rg` 自查与相关 typecheck/test 通过 |
