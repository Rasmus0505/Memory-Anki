---
编号: 09-01
标题: 为前端增加浅色/深色主题切换（暗色模式），覆盖全部 semantic token 并联动 PWA theme-color
类型: 新增
范围: 前端
优先级: P1
预估工作量: L（>8h）
依赖文档: 08-14（硬编码色板迁移；未完成时 FreestylePage 等页面的硬编码 zinc 类不会随主题变化，暗色下会出现"白块"）
状态: 已完成
负责代理: Codex
完成时间: 2026-07-09
---

# 09-01 暗色模式

## 1. 原始需求

当前前端**完全没有暗色模式**：`apps/web/src/index.css`（共 1690 行）中不存在任何 `.dark` 选择器，也没有 `prefers-color-scheme` 媒体查询（已用 `rg -n "dark|prefers-color-scheme" apps/web/src/index.css` 核实，0 处匹配）。但 token 体系已具备扩展基础——`index.css` 第 3-48 行的 `@theme` 块用 CSS 变量定义了完整的浅色暖色色板（primary 橙棕 `hsl(24 76% 43%)`），并包含 semantic token：`success/warning/error/info` 与 `memory-strong/medium/weak`。所有 Radix 封装组件（`shared/components/ui/`）都通过这些 token 上色，因此只需给 token 提供一套暗色值 + 一个 `html.dark` 切换机制，即可让绝大部分界面自动变暗。

手机通过 Tailscale 以 PWA 方式在夜间使用时，纯白界面刺眼，是真实的个人使用痛点（符合 1.md Karpathy 准则：解决自己真实遇到的问题，不做多余功能）。

已知遗留问题（本文档需处理或明确交给依赖文档）：

- `apps/web/src/features/freestyle/FreestylePage.tsx`（1652 行）中有 **44 行**使用了硬编码的 `zinc-*` Tailwind 色板类（用 `rg -n "\-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}" apps/web/src/features/freestyle/FreestylePage.tsx` 核实），这些类不会随 token 变化——迁移工作属于 **08-14**，本文档不改它们。
- `apps/web/src/index.css` 全文有 **43 处**裸 hex 颜色（`rg "#[0-9a-fA-F]{3,8}\b" apps/web/src/index.css` 核实），另有大量 `rgba(...)` 字面量（如第 52-62 行 body 背景渐变、第 81-94 行 `.memory-anki-mindmap-frame`）。本文档只为其中"整页级"的 body 背景与 mindmap 画布底色补 `.dark` 覆盖，其余零散 hex 的清理同样归 08-14。
- `apps/web/public/manifest.webmanifest` 第 8-9 行 `background_color`/`theme_color` 均为 `#020617`（深色 slate），与浅色应用不一致；`apps/web/index.html` 第 8 行 `<meta name="theme-color" content="#020617" />` 同样不一致。本文档一并修复并做主题联动。

## 2. 详细执行清单

> 约定：所有路径相对仓库根目录 `Memory-Anki/`。执行时**只允许**修改本清单点名的文件，禁止顺手改动 FreestylePage、各 feature 页面或其他任何文件。

### 步骤 1：在 index.css 注册 dark 变体并加入暗色色值表

打开 `apps/web/src/index.css`。当前第 1-3 行是：

```css
@import "tailwindcss";

@theme {
```

在 `@import "tailwindcss";` 之后、`@theme {` 之前**插入一行**（Tailwind v4 CSS-first 写法，让 `dark:` 前缀类跟随 `html.dark` 而不是系统媒体查询）：

```css
@custom-variant dark (&:where(.dark, .dark *));
```

然后找到 `@theme { ... }` 块的结束大括号（当前第 48 行，`--shadow-floating` 之后），在其**后面**插入下面的完整暗色色值表。暗色板设计原则：维持暖色调（hue 停留在 24-36 区间）、primary 在深底上提亮提饱和、semantic token 全部提供暗色版本、阴影改为纯黑基底：

```css
/* ===== 09-01 暗色主题：覆盖 @theme 生成的 CSS 变量 ===== */
.dark {
  --color-background: hsl(24 12% 9%);
  --color-foreground: hsl(36 20% 92%);
  --color-card: hsl(24 10% 12%);
  --color-card-foreground: hsl(36 20% 92%);
  --color-popover: hsl(24 10% 13%);
  --color-popover-foreground: hsl(36 20% 92%);
  --color-primary: hsl(26 82% 58%);
  --color-primary-foreground: hsl(24 30% 10%);
  --color-secondary: hsl(24 8% 18%);
  --color-secondary-foreground: hsl(36 16% 88%);
  --color-muted: hsl(24 8% 16%);
  --color-muted-foreground: hsl(30 8% 62%);
  --color-accent: hsl(28 20% 20%);
  --color-accent-foreground: hsl(36 16% 88%);
  --color-destructive: hsl(0 72% 58%);
  --color-destructive-foreground: hsl(0 0% 98%);
  --color-success: hsl(145 48% 48%);
  --color-success-foreground: hsl(150 40% 8%);
  --color-warning: hsl(34 90% 58%);
  --color-warning-foreground: hsl(30 40% 8%);
  --color-error: hsl(0 72% 58%);
  --color-error-foreground: hsl(0 0% 98%);
  --color-info: hsl(210 80% 62%);
  --color-info-foreground: hsl(215 40% 8%);
  --color-memory-strong: hsl(160 55% 45%);
  --color-memory-medium: hsl(34 90% 58%);
  --color-memory-weak: hsl(0 72% 62%);
  --color-border: hsl(24 8% 24%);
  --color-input: hsl(24 8% 26%);
  --color-ring: hsl(26 82% 58%);

  --shadow-soft: 0 1px 2px rgba(0, 0, 0, 0.4);
  --shadow-card: 0 1px 2px rgba(0, 0, 0, 0.45), 0 8px 24px rgba(0, 0, 0, 0.45);
  --shadow-popover: 0 18px 44px rgba(0, 0, 0, 0.6);
  --shadow-floating: 0 24px 72px rgba(0, 0, 0, 0.65);
}
```

注意：

- **不要**修改 `@theme` 块内的任何浅色值。
- **不要**在暗色表里加 `--color-radius` / `--radius-*`（圆角与主题无关）。
- `@theme` 会把变量输出到 `:root`，`.dark` 加在 `<html>` 上时与 `:root` 同为 html 元素，`.dark` 块写在后面即可覆盖（标准 shadcn/Tailwind v4 模式）。

自查点：`cd apps/web && npm run build` 通过；在浏览器控制台执行 `document.documentElement.classList.add('dark')` 后，卡片、按钮、边框应立即变为深色。

### 步骤 2：为 index.css 中整页级硬编码背景补 dark 覆盖

同一文件 `apps/web/src/index.css`：

2a. 第 52-62 行 body 背景使用了浅色 rgba 渐变字面量：

```css
body {
  margin: 0;
  font-family: var(--font-sans);
  background:
    radial-gradient(circle at top left, rgba(180, 83, 9, 0.06), transparent 26%),
    linear-gradient(180deg, rgba(248, 250, 252, 0.98), rgba(245, 245, 244, 0.96)),
    var(--color-background);
  ...
}
```

在步骤 1 加入的 `.dark { ... }` 块**之后**追加：

```css
.dark body {
  background:
    radial-gradient(circle at top left, rgba(217, 119, 6, 0.08), transparent 26%),
    linear-gradient(180deg, rgba(20, 16, 13, 0.98), rgba(24, 19, 16, 0.96)),
    var(--color-background);
}
```

2b. 第 81-94 行 `.memory-anki-mindmap-frame` 底色为 `#fafafa` 加浅色网格线。追加：

```css
.dark .memory-anki-mindmap-frame {
  border-color: hsl(24 8% 24%);
  background:
    radial-gradient(circle at 18% 12%, rgba(96, 165, 250, 0.06), transparent 30%),
    radial-gradient(circle at 82% 20%, rgba(167, 139, 250, 0.06), transparent 28%),
    linear-gradient(rgba(250, 250, 250, 0.05) 1px, transparent 1px),
    linear-gradient(90deg, rgba(250, 250, 250, 0.045) 1px, transparent 1px),
    hsl(24 10% 11%);
  background-size: auto, auto, 32px 32px, 32px 32px, auto;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.04),
    0 18px 44px rgba(0, 0, 0, 0.5);
}
```

2c. 其余 43 处裸 hex（扫描命令：`rg -n "#[0-9a-fA-F]{3,8}\b" apps/web/src/index.css`；rgba 扫描：`rg -n "rgba?\(" apps/web/src/index.css`）**本步骤不处理**，在本文档"进度记录"表中登记扫描结果，剩余项由 08-14 统一迁移。**不要**在本步骤逐条改写它们。

自查点：手动加 `dark` class 后，页面整体背景与 mindmap 画布均为深色，无刺眼白底大块（FreestylePage 的 zinc 硬编码区域除外——那是 08-14 的已知豁免）。

### 步骤 3：新建主题偏好模块

新建文件 `apps/web/src/shared/theme/themePreference.ts`，完整内容：

```ts
export type ThemePreference = 'light' | 'dark' | 'system'

export const THEME_STORAGE_KEY = 'memory-anki-theme'
export const THEME_UPDATED_EVENT = 'memory-anki-theme-updated'

/** 与 index.css 中 --color-background 对应的十六进制近似值，用于 meta theme-color */
export const LIGHT_THEME_COLOR = '#f9f7f3'
export const DARK_THEME_COLOR = '#1a1614'

export function isThemePreference(value: unknown): value is ThemePreference {
  return value === 'light' || value === 'dark' || value === 'system'
}

export function getStoredThemePreference(): ThemePreference {
  if (typeof window === 'undefined') return 'system'
  try {
    const raw = window.localStorage.getItem(THEME_STORAGE_KEY)
    return isThemePreference(raw) ? raw : 'system'
  } catch {
    return 'system'
  }
}

export function resolveTheme(preference: ThemePreference): 'light' | 'dark' {
  if (preference !== 'system') return preference
  if (typeof window === 'undefined') return 'light'
  return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
}

function updateThemeColorMeta(theme: 'light' | 'dark') {
  const meta = document.querySelector<HTMLMetaElement>('meta[name="theme-color"]')
  if (meta) {
    meta.content = theme === 'dark' ? DARK_THEME_COLOR : LIGHT_THEME_COLOR
  }
}

export function applyThemePreference(preference: ThemePreference) {
  if (typeof document === 'undefined') return
  const theme = resolveTheme(preference)
  document.documentElement.classList.toggle('dark', theme === 'dark')
  document.documentElement.style.colorScheme = theme
  updateThemeColorMeta(theme)
}

export function setThemePreference(preference: ThemePreference) {
  try {
    window.localStorage.setItem(THEME_STORAGE_KEY, preference)
  } catch {
    // localStorage 不可用时仅应用到当前会话
  }
  applyThemePreference(preference)
  window.dispatchEvent(new CustomEvent(THEME_UPDATED_EVENT, { detail: preference }))
}

/** 应用启动时调用：应用已存偏好，并在偏好为 system 时跟随系统变化 */
export function initializeTheme() {
  if (typeof window === 'undefined') return
  applyThemePreference(getStoredThemePreference())
  const media = window.matchMedia('(prefers-color-scheme: dark)')
  media.addEventListener('change', () => {
    if (getStoredThemePreference() === 'system') {
      applyThemePreference('system')
    }
  })
}
```

设计决策（**不要**改成别的方案）：

- 主题偏好**只存 localStorage，不新增后端 `ClientPreferences` 字段**（`apps/web/src/shared/api/contracts/profile.ts` 第 211-221 行）。主题是设备级偏好——桌面白天用浅色、手机夜间用深色是合理的分歧，跨设备同步反而有害；同时避免动后端（本文档范围是前端）。
- 不引入 `next-themes` 等依赖。

自查点：`npm run typecheck` 通过。

### 步骤 4：main.tsx 启动时初始化主题

打开 `apps/web/src/main.tsx`（当前共 13 行）。修改前：

```tsx
import './index.css'
import App from './app/App'
import { registerServiceWorker } from './pwa/registerServiceWorker'

createRoot(document.getElementById('root')!).render(
```

修改后（在 render 之前调用，尽早减少闪白）：

```tsx
import './index.css'
import App from './app/App'
import { registerServiceWorker } from './pwa/registerServiceWorker'
import { initializeTheme } from './shared/theme/themePreference'

initializeTheme()

createRoot(document.getElementById('root')!).render(
```

自查点：设置 localStorage `memory-anki-theme` 为 `"dark"` 后刷新，页面直接以深色渲染。

### 步骤 5：index.html 防闪白（FOUC）内联脚本 + 修正 theme-color

打开 `apps/web/index.html`：

5a. 第 8 行修改前：

```html
<meta name="theme-color" content="#020617" />
```

修改后（默认浅色值，运行时由 themePreference.ts 动态改写）：

```html
<meta name="theme-color" content="#f9f7f3" />
```

5b. 在 `<meta name="theme-color" ...>` 的**下一行**插入内联脚本（在主 bundle 加载前就给 html 打上 class，避免深色用户看到一闪而过的白屏）：

```html
<script>
  ;(() => {
    try {
      var t = localStorage.getItem('memory-anki-theme')
      var dark = t === 'dark' || ((t === null || t === 'system') && matchMedia('(prefers-color-scheme: dark)').matches)
      if (dark) {
        document.documentElement.classList.add('dark')
        document.querySelector('meta[name="theme-color"]').content = '#1a1614'
      }
    } catch (e) {}
  })()
</script>
```

**不要**改动 index.html 中已有的 PWA 启动诊断脚本（第 20-97 行）。

自查点：深色偏好下强刷新（Ctrl+Shift+R），首帧即为深色背景。

### 步骤 6：修正 manifest.webmanifest

打开 `apps/web/public/manifest.webmanifest`，第 8-9 行修改前：

```json
  "background_color": "#020617",
  "theme_color": "#020617",
```

修改后（manifest 是静态文件无法随主题切换，取浅色默认值与应用一致；安装后运行时状态栏颜色以页面里的 meta theme-color 为准）：

```json
  "background_color": "#f9f7f3",
  "theme_color": "#f9f7f3",
```

自查点：`apps/web/src/indexCss.pwa.test.ts` 等既有测试仍通过（`npm run test`）。

### 步骤 7：Profile 设置页加入"外观"入口

7a. 新建文件 `apps/web/src/features/profile/ThemeSettingsCard.tsx`，完整内容：

```tsx
import { useEffect, useState } from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/shared/components/ui/card'
import { Label } from '@/shared/components/ui/label'
import {
  getStoredThemePreference,
  setThemePreference,
  THEME_UPDATED_EVENT,
  type ThemePreference,
} from '@/shared/theme/themePreference'

const OPTIONS: Array<{ value: ThemePreference; label: string; description: string }> = [
  { value: 'light', label: '浅色', description: '暖色浅底（默认）' },
  { value: 'dark', label: '深色', description: '夜间使用不刺眼' },
  { value: 'system', label: '跟随系统', description: '随操作系统自动切换' },
]

export function ThemeSettingsCard() {
  const [preference, setPreference] = useState<ThemePreference>(() => getStoredThemePreference())

  useEffect(() => {
    const sync = () => setPreference(getStoredThemePreference())
    window.addEventListener(THEME_UPDATED_EVENT, sync)
    return () => window.removeEventListener(THEME_UPDATED_EVENT, sync)
  }, [])

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-base">外观</CardTitle>
      </CardHeader>
      <CardContent>
        <Label className="mb-2 block">主题</Label>
        <div className="flex flex-wrap gap-2">
          {OPTIONS.map((option) => (
            <button
              key={option.value}
              type="button"
              onClick={() => {
                setThemePreference(option.value)
                setPreference(option.value)
              }}
              className={`rounded-lg border px-4 py-2.5 text-left text-sm transition-colors ${
                preference === option.value
                  ? 'border-primary bg-primary/10 text-primary'
                  : 'border-border text-muted-foreground hover:border-primary/40 hover:text-foreground'
              }`}
            >
              <div className="font-medium">{option.label}</div>
              <div className="text-xs opacity-80">{option.description}</div>
            </button>
          ))}
        </div>
        <p className="mt-3 text-xs text-muted-foreground">
          主题偏好保存在当前设备（localStorage），桌面与手机 PWA 可各自设置。
        </p>
      </CardContent>
    </Card>
  )
}
```

7b. 打开 `apps/web/src/features/profile/ProfileSettingsPage.tsx`。该页第 160-179 行有一个自绘 tab 栏，tab 定义在第 161-165 行：

```tsx
{[
  { key: 'config', label: '复习配置', icon: Settings },
  { key: 'io', label: '导入导出', icon: Download },
  { key: 'shortcuts', label: '快捷键', icon: Keyboard },
].map(({ key, label, icon: Icon }) => (
```

**不新增 tab**（保持最小改动），改为在 `config` tab 的表单**上方**渲染外观卡片。找到第 183-184 行附近：

```tsx
) : tab === 'config' ? (
  <form onSubmit={handleSaveConfig} className="space-y-6">
```

改为：

```tsx
) : tab === 'config' ? (
  <div className="space-y-6">
    <ThemeSettingsCard />
    <form onSubmit={handleSaveConfig} className="space-y-6">
```

并在对应 `</form>` 后补 `</div>` 闭合；文件顶部 import 区新增：

```tsx
import { ThemeSettingsCard } from '@/features/profile/ThemeSettingsCard'
```

注意 tab 的 `setTab` 泛型 `'config' | 'io' | 'shortcuts'` **不需要改**。JSX 嵌套调整时不要动 form 内部任何字段。

自查点：`/profile` 复习配置 tab 顶部出现"外观"卡片，点击三个选项能即时切换主题且刷新后保持。

### 步骤 8：登记依赖与豁免

在本文档进度记录表中登记：08-14 未完成前，`FreestylePage.tsx` 的 44 行 zinc 类在暗色下仍显示浅色卡片，属预期豁免，不要在本文档内修它。

## 3. 测试验收标准

命令验证：

```
cd apps/web && npm run typecheck   # 期望：0 错误
cd apps/web && npm run test        # 期望：全部通过（含 indexCss.pwa.test.ts、serviceWorkerContract.test.ts）
cd apps/web && npm run build       # 期望：构建成功
```

行为验收（操作 → 期望现象）：

1. 打开 `/profile` → 复习配置 tab 顶部出现"外观"卡片，含 浅色/深色/跟随系统 三个选项。
2. 点击"深色" → 页面立即变深色（背景、卡片、边框、按钮、success/warning/info 徽标、memory-strong/medium/weak 记忆强度色全部为暗色版本），无需刷新。
3. 深色状态下强刷新（Ctrl+Shift+R）→ 首帧即深色，无白色闪屏。
4. 控制台检查 `document.querySelector('meta[name="theme-color"]').content` → 深色时为 `#1a1614`，浅色时为 `#f9f7f3`。
5. 选择"跟随系统"，切换 Windows 深浅色 → 页面在数秒内自动跟随。
6. 打开任一宫殿 `/palaces/:id` → mindmap 画布底色为深色网格，非白底。
7. 手机 PWA（Tailscale）验收：设置深色 → 关闭 PWA 重新打开 → 仍为深色；状态栏/标题栏颜色为深色（Android Chrome 可见）。
8. 已知豁免确认：`/freestyle` 页部分卡片仍为浅色（zinc 硬编码），等待 08-14 后消失。

回归检查：浅色模式下逐页浏览 `/freestyle`、`/dashboard`、`/palaces`、`/review`、`/profile`，外观与改动前一致（`@theme` 浅色值未被修改）；PWA 离线打开 `/freestyle` 仍正常（sw.js 未被本文档改动）。

## 4. 进度记录（交接用）

| 时间 | 执行者 | 动作 | 结果/备注 |
|---|---|---|---|
| - | - | 文档创建 | - |
| 2026-07-09 | Codex | 实现暗色主题 token 与 `html.dark` 切换 | `index.css` 新增 Tailwind v4 `dark` custom variant、完整 semantic token 暗色覆盖、body 与 mindmap frame 暗色背景覆盖；浅色 `@theme` 值未修改。 |
| 2026-07-09 | Codex | 实现设备级主题偏好与 PWA 联动 | 新增 `themePreference.ts`，使用 localStorage 保存 `light/dark/system`；`main.tsx` 启动初始化；`index.html` 新增防闪白脚本并联动 `meta[name="theme-color"]`；manifest 默认色改为浅色 `#f9f7f3`。 |
| 2026-07-09 | Codex | Profile 设置页加入外观卡片 | `ProfileSettingsPage.tsx` 的复习配置 tab 顶部新增 `ThemeSettingsCard`，点击浅色/深色/跟随系统可即时应用并刷新保持。 |
| 2026-07-09 | Codex | 扫描与豁免登记 | 当前扫描：`rg -n "#[0-9a-fA-F]{3,8}\b" apps/web/src/index.css` 为 41 处；`rg -n "rgba?\(" apps/web/src/index.css` 为 133 处；`rg -n "\-(slate\|zinc\|gray\|neutral\|stone)-[0-9]{2,3}" apps/web/src/features/freestyle/FreestylePage.tsx` 为 1 处。剩余硬编码颜色与 FreestylePage zinc/slate 类按本文依赖归 08-14，本文档不修。 |
| 2026-07-09 | Codex | 测试验证 | `cd apps/web && npm run typecheck` 通过；`npm run test -- src/shared/theme/themePreference.test.ts src/features/profile/ProfileSettingsPage.test.tsx src/indexCss.pwa.test.ts src/pwa/serviceWorkerContract.test.ts` 通过（4 files / 10 tests）；`npm run test` 通过；`npm run build` 通过（仅既有 ineffective dynamic import 警告）。 |
