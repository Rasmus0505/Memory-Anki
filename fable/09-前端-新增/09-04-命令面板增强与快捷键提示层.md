---
编号: 09-04
标题: 增强全局命令面板：最近访问、全量页面导航、真实快捷键显示，并为全局搜索预留接入位
类型: 新增
范围: 前端
优先级: P2
预估工作量: M（2-8h）
依赖文档: 无（为 06-01 全局搜索预留接入位，但不依赖它）
状态: 已完成
负责代理: Codex
完成时间: 2026-07-09
---

# 09-04 命令面板增强与快捷键提示层

## 1. 原始需求

`apps/web/src/app/shell/GlobalCommandPalette.tsx`（129 行，基于 cmdk 的 `CommandDialog`，Ctrl+K 唤起）现状核实：只有一个"常用操作"分组，共 **6 条命令**（第 31-71 行）：开始复习、新建宫殿、搜索宫殿、记忆宫殿、随心模式、仪表盘。存在的问题：

- `CommandShortcut` 列混用真实快捷键与英文别名（第 35 行 `shortcut: 'Review'`、第 53 行 `'Palaces'` 等不是快捷键，只有 `Ctrl+N` 和 `/` 是真的，见第 83-91 行的全局键盘监听）。
- 导航命令不全：`navSections`（`apps/web/src/app/shell/AppShell.tsx` 第 69-168 行）有 8 个区块，但面板缺英语听力、英语阅读、知识大纲、个人中心 4 个。
- 没有"最近访问"，回到刚看过的宫殿/课程要重新找。
- 06-01（全局搜索）将来要接入面板，目前没有预留位置。

## 2. 详细执行清单

> 约定：路径相对仓库根目录 `Memory-Anki/`。本文档不引入新依赖（cmdk 已有），不做全局"快捷键速查表"弹窗（个人项目快捷键少，面板内直接显示已够用——克制设计）。

### 步骤 1：从 AppShell 导出 navSections

打开 `apps/web/src/app/shell/AppShell.tsx`，第 69 行修改前：

```tsx
const navSections: NavSectionDefinition[] = [
```

修改后（仅加 `export`，其余不动）：

```tsx
export const navSections: NavSectionDefinition[] = [
```

同时把第 56-64 行的 `interface NavSectionDefinition` 也加上 `export`。**不要**改动 sections 的内容、顺序或 warmup 逻辑。

自查点：`npm run typecheck` 通过，AppShell 页面渲染无变化。

### 步骤 2：新建最近访问记录模块

新建文件 `apps/web/src/app/shell/recentVisits.ts`，完整内容：

```ts
export interface RecentVisit {
  /** 完整路径（含 search），如 /palaces/42 */
  path: string
  /** 展示名，如 "记忆宫殿 · /palaces/42" */
  label: string
  visitedAt: number
}

const STORAGE_KEY = 'memory-anki-recent-visits'
const MAX_ITEMS = 5

/** 不值得记录的路径（首页重定向、当前页由调用方排除） */
const IGNORED_PATHS = new Set(['/', '/timer-overlay'])

export function readRecentVisits(): RecentVisit[] {
  try {
    const raw = window.sessionStorage.getItem(STORAGE_KEY)
    if (!raw) return []
    const parsed = JSON.parse(raw) as unknown
    if (!Array.isArray(parsed)) return []
    return parsed.filter(
      (item): item is RecentVisit =>
        typeof item === 'object' &&
        item !== null &&
        typeof (item as RecentVisit).path === 'string' &&
        typeof (item as RecentVisit).label === 'string' &&
        typeof (item as RecentVisit).visitedAt === 'number',
    )
  } catch {
    return []
  }
}

export function recordRecentVisit(path: string, label: string) {
  if (IGNORED_PATHS.has(path)) return
  try {
    const existing = readRecentVisits().filter((item) => item.path !== path)
    const next: RecentVisit[] = [{ path, label, visitedAt: Date.now() }, ...existing].slice(0, MAX_ITEMS)
    window.sessionStorage.setItem(STORAGE_KEY, JSON.stringify(next))
  } catch {
    // sessionStorage 不可用时静默放弃（隐私模式等）
  }
}
```

设计决策：用 sessionStorage（会话级）而不是 localStorage——"最近访问"的价值主要在当前会话内跳转，跨会话历史属于过度设计。**不要**改成后端存储。

自查点：`npm run typecheck` 通过。

### 步骤 3：重写 GlobalCommandPalette

用下面的完整内容**覆盖** `apps/web/src/app/shell/GlobalCommandPalette.tsx`（保留原有 Ctrl+K/Ctrl+N/`/` 快捷键行为与 `isEditableTarget` 防误触逻辑，新增最近访问分组、页面导航分组、真实快捷键显示、06-01 接入位）：

```tsx
import { useEffect, useMemo, useState } from 'react'
import { Brain, History, Plus, Search } from 'lucide-react'
import { useLocation, useNavigate } from 'react-router-dom'
import {
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandShortcut,
} from '@/shared/components/ui/command'
import { navSections } from '@/app/shell/AppShell'
import { readRecentVisits, recordRecentVisit, type RecentVisit } from '@/app/shell/recentVisits'

function isEditableTarget(target: EventTarget | null) {
  const element = target instanceof Element ? target : null
  if (!element) return false
  const tagName = element.tagName.toLowerCase()
  return (
    tagName === 'input' ||
    tagName === 'textarea' ||
    tagName === 'select' ||
    Boolean(element.closest('[contenteditable="true"]'))
  )
}

export function GlobalCommandPalette() {
  const navigate = useNavigate()
  const location = useLocation()
  const [open, setOpen] = useState(false)
  const [recentVisits, setRecentVisits] = useState<RecentVisit[]>([])

  // 记录最近访问：以 navSections 的 label 命名当前 section
  useEffect(() => {
    const section = navSections.find((candidate) => candidate.matches(location.pathname))
    const label = section ? `${section.label} · ${location.pathname}` : location.pathname
    recordRecentVisit(`${location.pathname}${location.search}`, label)
  }, [location.pathname, location.search])

  // 动作类命令（真实快捷键才显示 shortcut）
  const actions = useMemo(
    () => [
      { label: '开始今日复习', shortcut: '', icon: Brain, run: () => navigate('/review') },
      { label: '新建宫殿', shortcut: 'Ctrl+N', icon: Plus, run: () => navigate('/palaces/new') },
      { label: '搜索宫殿', shortcut: '/', icon: Search, run: () => navigate('/palaces/list?focusSearch=true') },
    ],
    [navigate],
  )

  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => {
      if (event.defaultPrevented || isEditableTarget(event.target)) return
      const key = event.key.toLowerCase()
      const commandKey = event.ctrlKey || event.metaKey
      if (commandKey && key === 'k') {
        event.preventDefault()
        setOpen((current) => !current)
        return
      }
      if (event.ctrlKey && !event.metaKey && key === 'n') {
        event.preventDefault()
        navigate('/palaces/new')
        return
      }
      if (!event.ctrlKey && !event.metaKey && !event.altKey && !event.shiftKey && event.key === '/') {
        event.preventDefault()
        navigate('/palaces/list?focusSearch=true')
      }
    }

    window.addEventListener('keydown', handleKeyDown, true)
    return () => window.removeEventListener('keydown', handleKeyDown, true)
  }, [navigate])

  useEffect(() => {
    setOpen(false)
  }, [location.pathname, location.search])

  // 面板打开瞬间读取最近访问（排除当前页）
  useEffect(() => {
    if (!open) return
    setRecentVisits(
      readRecentVisits().filter((item) => item.path !== `${location.pathname}${location.search}`),
    )
  }, [open, location.pathname, location.search])

  const runAndClose = (run: () => void) => {
    run()
    setOpen(false)
  }

  return (
    <CommandDialog open={open} onOpenChange={setOpen}>
      <CommandInput placeholder="搜索操作或页面...（Ctrl+K）" />
      <CommandList>
        <CommandEmpty>没有找到对应操作。</CommandEmpty>

        {recentVisits.length > 0 ? (
          <CommandGroup heading="最近访问">
            {recentVisits.map((visit) => (
              <CommandItem
                key={visit.path}
                value={`recent ${visit.label}`}
                onSelect={() => runAndClose(() => navigate(visit.path))}
              >
                <History className="size-4" />
                <span className="truncate">{visit.label}</span>
              </CommandItem>
            ))}
          </CommandGroup>
        ) : null}

        <CommandGroup heading="操作">
          {actions.map((action) => {
            const Icon = action.icon
            return (
              <CommandItem
                key={action.label}
                value={action.label}
                onSelect={() => runAndClose(action.run)}
              >
                <Icon className="size-4" />
                <span>{action.label}</span>
                {action.shortcut ? <CommandShortcut>{action.shortcut}</CommandShortcut> : null}
              </CommandItem>
            )
          })}
        </CommandGroup>

        <CommandGroup heading="页面">
          {navSections.map((section) => {
            const Icon = section.icon
            return (
              <CommandItem
                key={section.key}
                value={`page ${section.label}`}
                onSelect={() => runAndClose(() => navigate(section.to))}
              >
                <Icon className="size-4" />
                <span>{section.label}</span>
              </CommandItem>
            )
          })}
        </CommandGroup>

        {/*
          06-01 全局搜索接入位：
          实现 06-01 时在此处新增 <CommandGroup heading="全局搜索">，
          将 CommandInput 的关键词透传给搜索 API（防抖 ≥200ms），
          结果项 onSelect 后 navigate 到对应宫殿/知识点。
          在那之前保持此注释，不要渲染空分组。
        */}
      </CommandList>
    </CommandDialog>
  )
}
```

注意：

- `value` 加 `recent` / `page` 前缀是为了避免 cmdk 把同名项（如"最近访问"里的记忆宫殿与"页面"里的记忆宫殿）判为重复。
- **不要**给纯导航项编造快捷键；`CommandShortcut` 只出现在真实全局快捷键（Ctrl+N、/）上。
- **不要**在此文件里 import features 层任何内容（架构边界：app 层可用 shared 与 app 内部模块）。

自查点：`npm run typecheck`；Ctrl+K 打开面板可见 操作/页面 两组（浏览过 ≥2 页后出现"最近访问"组）。

### 步骤 4：确认循环依赖无问题

`GlobalCommandPalette` import `AppShell` 的 `navSections`，而 `AppShell.tsx` 第 44 行 import `GlobalCommandPalette`——这是模块级循环引用。ES module 下常量提升可用，但为稳妥起见执行后必须验证：`npm run dev` 启动无 `Cannot access 'navSections' before initialization` 报错。**如果**出现该报错，则执行替代方案：把 `NavSectionDefinition` 接口与 `navSections` 数组整体搬到新文件 `apps/web/src/app/shell/navSections.ts`（连同其依赖的 preload/prefetch import 一起搬），`AppShell.tsx` 与 `GlobalCommandPalette.tsx` 都从新文件 import。搬移时不得修改数组内容。

自查点：dev 启动 + 构建产物 preview 均无运行时报错。

## 3. 测试验收标准

命令验证：

```
cd apps/web && npm run typecheck   # 期望：0 错误
cd apps/web && npm run test        # 期望：全部通过
cd apps/web && npm run build      # 期望：构建成功
```

行为验收（操作 → 期望现象）：

1. 按 Ctrl+K → 面板打开，见"操作"（3 项）与"页面"（8 项：仪表盘/随心模式/记忆宫殿/英语听力/英语阅读/知识大纲/复习/个人中心）两个分组。
2. 依次访问 `/palaces` → `/review` → 按 Ctrl+K → 顶部出现"最近访问"分组，含"记忆宫殿 · /palaces"（当前页 /review 不在列表中）；点击即跳转。
3. 检查快捷键列：只有"新建宫殿 Ctrl+N"和"搜索宫殿 /"显示快捷键，其余项无快捷键文本。
4. 输入"英语" → 过滤出英语听力、英语阅读两项，回车跳转成功。
5. 在输入框/textarea 聚焦时按 `/` 或 Ctrl+N → 不触发全局快捷键（isEditableTarget 保护未回归）。
6. 手机 PWA：无实体键盘无法唤起面板属预期，不做手机入口（克制设计）；确认面板不会意外弹出。

回归检查：Ctrl+K 开/关、路由切换自动关闭面板、Ctrl+N 新建宫殿、`/` 聚焦宫殿搜索均与改动前一致；侧边栏 hover 预热（`warmNavSection`）不受 navSections 导出影响。

## 4. 进度记录（交接用）

| 时间 | 执行者 | 动作 | 结果/备注 |
|---|---|---|---|
| - | - | 文档创建 | - |
| 2026-07-09 | Codex | 实现命令面板增强 | 抽离 `navSections.ts` 避免 `AppShell`/`GlobalCommandPalette` 循环 import；新增 sessionStorage 最近访问；面板拆分为最近访问/操作/页面分组，仅显示真实快捷键；保留 06-01 全局搜索注释接入位；新增 shell 单测。验证命令见最终汇报。 |
| 2026-07-09 | Codex | 复核实现并补齐状态 | 确认 `navSections` 已抽离、最近访问已接入、页面分组覆盖 8 个导航项、快捷键列仅显示真实快捷键。验证通过：`npm run test -- src/app/shell/GlobalCommandPalette.test.tsx src/app/shell/AppShell.test.tsx src/app/shell/AppShell.residency.test.tsx`、`npm run typecheck`、`npm run build`。测试输出仍有既有 Radix Dialog 标题/描述提示与 AppShell act warning，不影响通过。 |
