---
编号: 04-04
标题: 把 11 个一次性 1000 题库数据管线脚本归档到 tools/archive/1000-quiz/ 并补 README
类型: 删减
范围: 功能
优先级: P2
预估工作量: S（<2h）
依赖文档: 无
状态: 未开始
负责代理: 无
完成时间: 无
---

# 04-04 归档一次性 1000 题库工具链脚本

## 1. 原始需求

`tools/` 共 23 个脚本，其中 11 个是围绕"考研 1000 题"题库导入的一次性数据管线（OCR → 定位 → 渲染 → 合并 → 清单 → 审计 → 修复 → 回填 → 重跑计划），与日常启动/备份/架构检查脚本混在一起，干扰对 tools 目录的认知。经核实（2026-07-08）：

- 11 个脚本全部存在：`ocr_1000_questions.py`、`locate_1000_quiz_pages.py`、`render_1000_question_pages.py`、`merge_1000_page_drafts.py`、`build_1000_questions_manifest.py`、`audit_1000_quiz_bank.py`、`repair_1000_quiz_bank_local.py`、`backfill_1000_quiz_ocr_sources.py`、`build_1000_quiz_rerun_plan.py`、`run_1000_quiz_rerun_plan.py`、`save_1000_quiz_preview_results.py`。
- 启动链（`start-desktop.bat`/`start-pwa.bat` → `tools/desktop_launcher.ps1`/`pwa_launcher.ps1` → `dev_server.py`/`pwa_server.py`）与 CI（`.github/workflows/ci.yml`）均不引用任何 1000 脚本。
- 脚本间唯一依赖：`backfill_1000_quiz_ocr_sources.py` 第 17 行 `from repair_1000_quiz_bank_local import ...`（同目录兄弟导入，两者必须一起移动）。
- `tools/check_architecture.py` 的 `BASELINE_PERSONAL_PATH_TOOLS`（127–136 行）以**相对路径字符串**豁免了其中 7 个脚本的"个人绝对路径"检查，且 `check_tool_personal_paths`（605–618 行）用 `rglob` **递归**扫描 tools/ 全目录——归档后新路径仍会被扫到，基线必须同步改路径。
- 现状注意：`python tools/check_architecture.py` **当前本来就是失败的**（实测退出码 1，共 9 条错误，其中与本文档相关的是 `repair_1000_quiz_bank_local.py` 与 `backfill_1000_quiz_ocr_sources.py` 两条——它们含 `D:\` 个人路径却不在基线里）。本次归档顺带把这两条修掉（加入基线），但**不许**修其余 7 条无关错误（超长文件、破坏性迁移、organize_memory_anki_docs.py）。

目标：11 个脚本移入 `tools/archive/1000-quiz/`，加 README 说明用途与执行顺序，**不删除任何脚本**。

## 2. 详细执行清单

### 步骤 0：移动前的安全检查清单（必须先做）

```powershell
# 检查 1：启动脚本与 CI 不引用 1000 脚本
rg "_1000_" --glob "*.bat" --glob "*.ps1" --glob "*.yml" --glob "*.json" --glob "!**/package-lock.json" .
# 期望输出：空（ralph/prd.json 里只有 check_architecture 相关文案，不含 1000 脚本）

# 检查 2：apps/ 代码不 import 这些脚本
rg "import.*_1000_|from.*_1000_" apps
# 期望输出：空

# 检查 3：脚本间依赖只有一条
rg "^from |^import " tools/*.py | rg "1000"
# 期望输出：仅 backfill_1000_quiz_ocr_sources.py 的 from repair_1000_quiz_bank_local import ...

# 检查 4：记录当前 check_architecture 失败清单（作为改动前基线）
python tools/check_architecture.py
# 期望输出：exit 1，9 条错误；把输出原样贴到第 4 节进度表备注里
```

### 步骤 1：建目录并用 git mv 移动 11 个脚本

```powershell
mkdir tools/archive/1000-quiz
git mv tools/ocr_1000_questions.py           tools/archive/1000-quiz/
git mv tools/locate_1000_quiz_pages.py       tools/archive/1000-quiz/
git mv tools/render_1000_question_pages.py   tools/archive/1000-quiz/
git mv tools/merge_1000_page_drafts.py       tools/archive/1000-quiz/
git mv tools/build_1000_questions_manifest.py tools/archive/1000-quiz/
git mv tools/audit_1000_quiz_bank.py         tools/archive/1000-quiz/
git mv tools/repair_1000_quiz_bank_local.py  tools/archive/1000-quiz/
git mv tools/backfill_1000_quiz_ocr_sources.py tools/archive/1000-quiz/
git mv tools/build_1000_quiz_rerun_plan.py   tools/archive/1000-quiz/
git mv tools/run_1000_quiz_rerun_plan.py     tools/archive/1000-quiz/
git mv tools/save_1000_quiz_preview_results.py tools/archive/1000-quiz/
```

不要做什么：
- 不要移动 `tools/import_manual_quiz_texts.py`——它虽然也在个人路径基线里，但不属于 1000 题库管线（处理的是手工 OCR 文本导入），留在 tools/。
- 不要移动 `organize_memory_anki_docs.py`（见 04-07 文档的范围）、`check_architecture.py`、`dev_server.py` 等其余 12 个脚本。
- 不要修改任何脚本内容（包括里面写死的 `D:\考研（丹丹）\1000题` 等个人路径——归档脚本保留原样）。

自查点：`(Get-ChildItem tools/*.py).Count` 比移动前少 11；`Get-ChildItem tools/archive/1000-quiz/*.py` 恰好 11 个。

### 步骤 2：同步 check_architecture.py 的基线路径

打开 `tools/check_architecture.py`，找到第 127–136 行：

```python
# 修改前
BASELINE_PERSONAL_PATH_TOOLS = {
    "tools/audit_1000_quiz_bank.py",
    "tools/build_1000_questions_manifest.py",
    "tools/import_manual_quiz_texts.py",
    "tools/locate_1000_quiz_pages.py",
    "tools/merge_1000_page_drafts.py",
    "tools/ocr_1000_questions.py",
    "tools/render_1000_question_pages.py",
    "tools/run_1000_quiz_rerun_plan.py",
}
```

改为（7 个改前缀 + 补上此前遗漏、实际含 `D:\` 路径的 repair 与 backfill 两个；`import_manual_quiz_texts.py` 路径不变）：

```python
# 修改后
BASELINE_PERSONAL_PATH_TOOLS = {
    "tools/archive/1000-quiz/audit_1000_quiz_bank.py",
    "tools/archive/1000-quiz/backfill_1000_quiz_ocr_sources.py",
    "tools/archive/1000-quiz/build_1000_questions_manifest.py",
    "tools/archive/1000-quiz/locate_1000_quiz_pages.py",
    "tools/archive/1000-quiz/merge_1000_page_drafts.py",
    "tools/archive/1000-quiz/ocr_1000_questions.py",
    "tools/archive/1000-quiz/render_1000_question_pages.py",
    "tools/archive/1000-quiz/repair_1000_quiz_bank_local.py",
    "tools/archive/1000-quiz/run_1000_quiz_rerun_plan.py",
    "tools/import_manual_quiz_texts.py",
}
```

说明：`build_1000_quiz_rerun_plan.py` 与 `save_1000_quiz_preview_results.py` 不含个人绝对路径（已用 drive-letter 正则核实），无需入基线。

自查点：`python tools/check_architecture.py` 的错误列表中不再出现任何 `tools/` 前缀的 1000 脚本条目；错误总数从 9 降为 7（剩余 7 条是改动前就存在的无关错误，不许顺手修）。

### 步骤 3：写归档 README

新建 `tools/archive/1000-quiz/README.md`，内容要点（完整写入）：

```markdown
# 1000 题库一次性数据管线（已归档）

这批脚本是 2025 年把《考研政治 1000 题》纸质题库导入 Memory Anki 的一次性管线，
数据已导入完成，日常运行不需要它们。归档保留仅为追溯与将来导入类似题库时参考。

## 管线顺序
1. locate_1000_quiz_pages.py    —— 在源 PDF 中定位题目页码
2. render_1000_question_pages.py —— 渲染题目页为图片
3. ocr_1000_questions.py        —— 对题目页做 OCR
4. merge_1000_page_drafts.py    —— 合并分页草稿
5. build_1000_questions_manifest.py —— 生成题目清单 manifest
6. audit_1000_quiz_bank.py      —— 审计导入后的题库质量
7. repair_1000_quiz_bank_local.py —— 本地修复题库（含批准的补充题数据）
8. backfill_1000_quiz_ocr_sources.py —— 回填 OCR 来源（依赖同目录 repair 脚本）
9. build_1000_quiz_rerun_plan.py / run_1000_quiz_rerun_plan.py —— 构建并执行重跑计划
10. save_1000_quiz_preview_results.py —— 保存预览结果

## 注意
- 脚本内写死了当时的个人绝对路径（如 D:\考研（丹丹）\1000题、D:\BaiduSyncdisk\...），
  换设备/换数据源需自行修改；这些路径已在 tools/check_architecture.py 的
  BASELINE_PERSONAL_PATH_TOOLS 中豁免。
- backfill_1000_quiz_ocr_sources.py 从同目录 import repair_1000_quiz_bank_local，
  两个文件必须放在一起。
- 直接操作 SQLite 数据库文件，运行前务必先备份 memory_palace.db。
```

自查点：README 存在且被 git add。

### 回滚方式

```powershell
# 未提交：git 会把 git mv 记录在暂存区
git reset
git checkout -- tools
Remove-Item -Recurse tools/archive

# 已提交：
git revert <提交 hash>
```

## 3. 测试验收标准

可执行命令：

```powershell
# 1. 架构检查：1000 脚本相关错误清零，无新增错误
python tools/check_architecture.py
# 期望：错误从 9 条降到 7 条，且 7 条与改动前基线（步骤 0 检查 4 的记录）逐条一致

# 2. 归档内兄弟导入仍可用（只验证 import，不执行主流程）
cd tools/archive/1000-quiz
python -c "import repair_1000_quiz_bank_local" 
# 期望：无 ImportError（若因缺第三方依赖报 ModuleNotFoundError 非 repair 本身，可接受并记录）

# 3. 后端测试与前端不受影响（tools 移动不应触碰 apps）
cd ../../../apps/api && python -m pytest   # 期望：与改动前同样的通过结果
```

行为验收：

- `start-desktop.bat` 启动 → 前后端正常起服务（验证启动链未被误伤）。
- `start-pwa.bat` / `stop-pwa.bat` 正常。

回归检查：

- `git status` 中除 `tools/archive/1000-quiz/`、`tools/check_architecture.py` 外无其他改动。
- `tools/import_manual_quiz_texts.py` 仍在 `tools/` 原位。

## 4. 进度记录（交接用）

| 时间 | 执行者 | 动作 | 结果/备注 |
|---|---|---|---|
| 2026-07-08 | fable 文档代理 | 文档创建；核实 11 脚本存在、唯一脚本间依赖、check_architecture 基线机制与当前 9 条既有失败 | 待执行 |
