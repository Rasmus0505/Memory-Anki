/**
 * @deprecated Prefer mounting EnglishLookupPanel + LookupAnchor from useMindMapEnglishMode.
 * Kept as a thin re-export shell so any residual imports compile.
 */
export function MindMapEnglishModeChrome() {
  return null
}
