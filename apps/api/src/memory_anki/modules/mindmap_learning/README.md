# Mind-map Learning

Recall events, mastery projections, and manual node labels.

- References palace nodes by `palace_id` and stable `node_uid`.
- Does not load, normalize, save, or mutate editor documents.
- Keeps the existing `/api/v1/mindmap/*` HTTP routes for client compatibility.
