@echo off
rem Repo-local runtime environment variables.
rem After cloning on another device, this file provides the same defaults.

set "MEMORY_ANKI_HOME=%~dp0repo-data"

rem Uncomment and fill these values to enable image-to-mindmap import.
rem set "DASHSCOPE_API_KEY=your-dashscope-key"
rem set "DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1"
rem set "DASHSCOPE_VISION_MODEL=qwen3-vl-flash"
