# Legacy Repo Data

该目录不再作为 Memory Anki 的正式运行数据目录。

- 正式运行数据默认位于 `%LOCALAPPDATA%/MemoryAnki/data`
- 可通过 `MEMORY_ANKI_HOME` 覆盖运行目录
- 仓库根 `data/` 仅保留为旧版本数据迁移来源或人工备份暂存区

如果这里仍有旧数据库、附件或备份，请先启动当前版本完成迁移，再决定是否手工清理本地文件。
